## 题目：审查下载辅助函数，不要修改代码

Case ID: `05-security-review`

请对 `src/download.mjs` 做聚焦的安全与正确性审查。报告你能从当前代码直接证明的问题，并按严重程度排序。每个问题必须包含：

- 文件与行号；
- 可触发的具体输入或场景；
- 实际影响；
- 简洁修复方向。

这是代码审查任务，不是实现任务。除必须创建的 `LEVELUP_RESULT.md` 外，不得修改任何项目文件。不要把纯风格偏好列为问题。
