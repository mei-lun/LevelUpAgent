## 题目：修复 TTL Cache 边界错误

Case ID: `01-ttl-cache`

项目中的 `TtlCache` 在基本场景下可以工作，但生产中发现以下行为不正确：

- 缓存值为 `0`、`false` 或空字符串时，`get` 必须仍能返回该值；
- 当 `now` 恰好等于过期时间时，该条目应视为已经过期，并从缓存中删除；
- 不得改变现有公开 API；
- 不要修改现有测试文件。

请修复实现。项目应继续通过现有测试。
